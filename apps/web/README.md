# Web frontend (Phase 5 UI)

Next.js or similar SPA: market tables with AI score columns, tooltips, AI vs baseline toggle, parameter suggestions panel.

API base URL: configure to the shared backend (e.g. `http://localhost:8000`).
